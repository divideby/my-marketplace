# day

Плагин для ежедневного планирования с задачами из Obsidian.

## Назначение

Автоматизация "протокола дня":
- Утреннее планирование с обзором всех задач
- Генерация расписания в формате DataviewJS
- Интерактивное сопровождение в течение дня
- Обработка результатов встреч

## Требования

Запускай Claude Code из корня Obsidian vault'а.

## Структура

```
day/
├── .claude-plugin/
│   └── plugin.json
├── commands/
│   ├── today.md      # /day:today — утреннее планирование
│   ├── checkin.md    # /day:checkin — чекин и следующая задача
│   ├── monkey.md     # /day:monkey — расщепить задачу
│   ├── daily.md      # /day:daily — daily standup
│   ├── close.md      # /day:close — закрытие дня
│   └── inbox.md      # /day:inbox — обработка входящих
├── agents/
│   └── meeting-processor.md  # Обработка встреч
├── skills/
│   └── obsidian-vault/
│       ├── SKILL.md
│       └── references/
│           └── schedule-template.js
├── settings/
│   └── patterns.example.md   # Шаблон для паттернов
└── scripts/
    └── parse-tasks.py
```

## Команды

| Команда | Описание |
| ------- | -------- |
| `/day:today` | Утреннее планирование: показать задачи → спросить фокусы → сгенерировать расписание |
| `/day:checkin` | Чекин: что сделал → отметить done → получить следующую задачу |
| `/day:monkey` | Расщепить задачу: когда застрял — разбить на атомарные шаги |
| `/day:daily` | Подготовка к daily по проекту: смотрит заметки → предлагает что сказать |
| `/day:close` | Закрытие дня: итоги → перенос невыполненных → обогащение бэклога |
| `/day:inbox` | Обработка входящих: рассортировать задачи без дат по нутриентам |

## Агенты

| Агент | Триггер |
| ----- | ------- |
| `meeting-processor` | "встреча прошла", "закончили звонок", "вот расшифровка" |

## Workflow

### Утро: `/day:today`

1. **Загружает паттерны** из `settings/patterns.md` (типичные времена)
2. **Что уже сделано** — утренняя рутина до планирования
3. **Собирает задачи** — due today, inbox без дат, просроченные
4. **Фиксированные события** — встречи/события которые НЕЛЬЗЯ перенести (📌 anchors)
5. **Подготовка к встречам** — что нужно подготовить к каждой встрече (🔶)
6. **Фокусы дня** — приоритеты для свободного времени
7. Генерирует DataviewJS-расписание с учётом всех ограничений

### В течение дня: `/day:checkin`

1. Спрашивает результат работы
2. Отмечает задачи выполненными
3. Предлагает следующую задачу по расписанию

### Когда застрял: `/day:monkey`

"Режим обезьянки" — выполняешь задачи без глубокого обдумывания. Но если задача не атомарная — застреваешь.

1. Определяет текущую задачу
2. Диагностирует причину: непонятно с чего начать? слишком большая? ждёшь информацию?
3. Расщепляет на микро-шаги или даёт конкретный первый шаг

**Критерий атомарности:** можно начать за 2 секунды, не думая.

### После встречи: agent `meeting-processor`

1. Активируется при упоминании встречи
2. Извлекает action items
3. Создаёт задачи в daily note

### Вечер: `/day:close`

1. Показывает итоги дня (выполнено / не выполнено)
2. Спрашивает по каждой невыполненной задаче куда перенести
3. Проверяет заметки встреч на неучтённые action items
4. Обновляет расписание (✅ у выполненных)

### По необходимости: `/day:inbox`

Когда накопились необработанные входящие:

1. Собирает задачи без дат из vault
2. Проверяет каждую по 4 критериям формулировки
3. Помогает переформулировать размытые задачи
4. Сортирует на 4 нутриента: задача, проект, информация, встреча
5. Раскладывает по соответствующим файлам

## Настройки

Паттерны хранятся в vault'е пользователя: `.claude/day-patterns.md`

Содержит:

- **Утренняя рутина** — что делается ДО планирования
- **Фиксированные события** — регулярные события в одно время
- **Временные слоты** — когда свободно от встреч, обед

**Установка**: скопируй `settings/patterns.example.md` в свой vault как `.claude/day-patterns.md` и отредактируй.

## Скрипты

### parse-tasks.py

```bash
# Задачи на сегодня
python3 scripts/parse-tasks.py --due-today

# Просроченные
python3 scripts/parse-tasks.py --overdue

# Без даты (inbox)
python3 scripts/parse-tasks.py --no-date

# Все незавершённые
python3 scripts/parse-tasks.py --undone
```

## Формат задач

```markdown
- [ ] Описание задачи ⏫ #task 📅 2026-01-14
```

- `⏫` / `🔼` / `🔽` — приоритет
- `⚡` — быстрая задача
- `#task` — обязательный тег
- `#wait` — ожидание ответа
- `📅 YYYY-MM-DD` — дата

## Расписание

Цветовая схема:

- `#4ade8033` — еда (зелёный)
- `#c4b5fd44` — рутины (лавандовый)
- `#60a5fa44` — deep work (синий)
- `#a78bfa44` — встречи (фиолетовый)
- `#fcd34d44` — подготовка к встречам (золотой)
- `#fbbf2444` — тренировки (янтарный)
- `#67e8f933` — здоровье (голубой)
- `#f9a8d433` — перерывы (розовый)

<claude-mem-context>
# Recent Activity

<!-- This section is auto-generated by claude-mem. Edit content outside the tags. -->

### Jan 17, 2026

| ID | Time | T | Title | Read |
|----|------|---|-------|------|
| #752 | 5:25 PM | 🟣 | Day Inbox Command Implementation Plan Created | ~516 |
</claude-mem-context>